#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int main() {
  int N;
  cin >> N;
  double V0, a, h1, h2, x1, g, r, t, y;
  g = 9.8;
  r = acos(-1) /180;
  t = x1 / V0/ cos (a*r);
  y - V0 * t * sin(a * r)- (0.5 *g * (t * t));

  
  for (int i = 0; i < N; i++)
    {
      cin >> V0 >> a >> h1 >> h2;
      
      
    }
  
}